# =================================
# VARIABLES DE ENTORNO REQUERIDAS
# =================================

# URL completa de la API REST de WordPress.
# Se usa en el servidor, por lo que NO debe llevar el prefijo NEXT_PUBLIC_.
WORDPRESS_API_URL=http://tu-wordpress.local/wp-json

# =================================
# VARIABLES DE ENTORNO OPCIONALES
# =================================

# URL base del sitio frontend. Útil para SEO (metadatos, sitemaps).
BASE_URL=http://localhost:3000

# Slug del tema activo en WordPress. Usado para obtener estilos globales.
WP_THEME_SLUG=twentytwentyfour

# Token secreto para asegurar los webhooks de revalidación bajo demanda (On-Demand ISR).
# REVALIDATE_SECRET=tu_token_secreto_muy_largo

# ID de Google Tag Manager para analíticas.
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX